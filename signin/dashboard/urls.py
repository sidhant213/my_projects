from django.urls import path
from dashboard.views import *

urlpatterns = [
 
]
# This file defines the URL patterns for the dashboard app, allowing users to view, edit, and delete user profiles.
# The `user_list` view displays all users, `user_edit` allows editing a specific user by ID, and `user_delete` handles user deletion.